interface-info:
  path: /api/users
  requestType: delete
  dataPlaceIn: body
default-test-data:
  # 正确删除用例1
  - describe: "正确删除用例1"
    data: "[3,4]"
    excepted:
      statusCode: 200

  # 正确删除用例2
  - describe: "正确删除用例2"
    data: "[]"
    excepted:
      statusCode: 200

  # 删除没有的用户
  - describe: "删除没有的用户"
    data: "[18,19]"
    excepted:
      statusCode: 400

  # 删除中存在没有的用户
  - describe: "删除中存在没有的用户"
    data: "[5,18]"
    excepted:
      statusCode: 400

  # 请求参数格式错误
  - describe: "请求参数格式错误"
    data: "5]"
    excepted:
      statusCode: 400
      message: JSON parse error

  # 请求参数格式错误
  - describe: "请求参数格式错误"
    data: "[5"
    excepted:
      statusCode: 400
      message: JSON parse error

#  # 删除用户时，禁止删除自己
#  - describe: "删除禁止删除的管理员用户"
#    data: "[1]"
#    excepted:
#      statusCode: 400